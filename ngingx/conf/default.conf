server {
    listen 80;
    client_max_body_size 50m;
    location /api/ {
        proxy_pass         http://fastapi_bt24:8888;
    }
}

server {
    listen                  443 ssl;
    ssl_certificate         /etc/nginx/cert/server.crt;
    ssl_certificate_key     /etc/nginx/cert/server.key;
    ssl on;
    client_max_body_size 50m;
    location /api/ {
        proxy_pass         http://fastapi_bt24:8888;
    }
}